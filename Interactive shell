
python -c "import pty; pty.spawn('/bin/bash');"

CTRL-Z to background the shell 

echo $TERM 
stty -a 
	<< get the rows and columns from the output>>
stty raw -echo 
fg 
reset 
export SHELL=bash
export TERM=xterm256-color 
stty rows <> columns <> 
